# Copy to .env and fill with your GitHub OAuth App credentials
# GitHub OAuth App: Settings → Developer settings → OAuth Apps → New OAuth App
# Authorization callback URL should exactly match your host, e.g.:
#   http://localhost:8080/api/auth/oauth/github/callback

OAUTH_GITHUB_CLIENT_ID=
OAUTH_GITHUB_CLIENT_SECRET=
# Optional: override if your host/port differs from localhost:8080
OAUTH_GITHUB_REDIRECT_URL=http://localhost:8080/api/auth/oauth/github/callback

# Optional cookie/session tuning
COOKIE_SAMESITE=lax
COOKIE_SECURE=false
SESSION_COOKIE_NAME=trellolite_sess
SESSION_TTL=336h
